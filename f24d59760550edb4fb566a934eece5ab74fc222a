lisa-design:
- Kept SSP API endpoints, route names, store semantics, emitted event names, and JSON download payload handling unchanged; the refactor is presentation-focused.
- Preserved the existing disabled System Characteristics Edit behavior instead of inventing a new metadata/characteristics update endpoint that is not present in this UI.
- Used volt `InputText`/`Textarea` across SSP forms, but retained the legacy numeric `FormInput` only for component protocol port range fields because volt `InputText` is string-model typed and OSCAL ports are numeric.
- Removed the System Implementation dashboard metrics and component distribution chart rather than restyling them, prioritizing the list workflows that users act on.
- Guarded `StatementByComponent` component fetches behind an SSP UUID watch so no `/system-security-plans/undefined/...` request is constructed before parent data is ready.
- Left risk widgets and Compliance/Risks routes untouched as separately owned surfaces.

lisa-selfreview: pass 1 (verdict=request_changes)
- [medium] src/views/system-security-plans/SystemSecurityPlanControlImplementationView.vue:405 (correctness vs. the actual SDK/library): The SSP control implementation page still imports the control-implementations StatementByComponent partial, while the guarded fetch change in this branch was applied to src/views/system-security-plans/partials/StatementByComponent.vue, which is only imported by its new test. The live component still builds its component URL from useSystemStore().system.securityPlan?.uuid, so direct navigation with no active SSP can still request /system-security-plans/undefined/... and viewing a non-active SSP can fetch component details from the wrong plan.
- [medium] src/views/system-security-plans/partials/StatementByComponent.vue:36 (correctness vs. the actual SDK/library): The new guarded watcher uses immediate: false. If this partial is mounted after ssp.uuid and byComponent.componentUuid are already available, which is the normal pattern when the parent gates rendering on ssp.uuid, componentUrl never changes and fetchComponent is never called; because the template is guarded by v-if="component", the by-component row renders nothing.
