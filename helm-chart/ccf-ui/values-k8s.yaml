# Values for in-cluster deployment (frontend connecting to backend service)
global:
  environment: dev

ui:
  image:
    pullPolicy: Never
  
  config:
    # For same namespace deployment - uses the backend service name
    apiUrl: "http://ccf-api:8080"
  
  env:
    NODE_ENV: "development"