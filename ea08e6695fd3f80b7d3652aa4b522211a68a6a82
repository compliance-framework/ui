lisa-addressed:
---
Comment (src/views/admin/NotificationsView.vue:1027):
`loadJobs()` sets `jobsError` for both fresh loads and cursor-based pagination. Since the Deliveries table is gated on `!jobsError`, a failed “Load More” request will hide the entire existing jobs table even though `jobs` still contains prior results. Consider only setting `jobsError` (and clearing `jobs`) for non-cursor loads, and handling cursor failures separately (e.g., keep the table visible and show an inline pagination error or toast).
Implementation guidance: In src/views/admin/NotificationsView.vue, keep the existing initial-load jobsError behavior and the !jobsError table gate, but split cursor pagination failures into a separate state so an unsuccessful Load More request does not hide already loaded jobs. Add a new ref such as jobsPaginationError = ref<string | null>(null) near jobsError. In loadJobs(cursor?: string), clear jobsPaginationError at the start of a cursor load, and clear both jobsError and jobsPaginationError for a fresh non-cursor load. In the catch block, if cursor is present, do not clear jobs, do not clear jobsNextCursor, and do not set jobsError; instead set jobsPaginationError to getApiErrorMessage(error, 'More notification delivery jobs are unavailable.'). If cursor is absent, preserve the existing behavior of clearing jobs/jobsNextCursor and setting jobsError. In the Deliveries template, render jobsPaginationError as a small inline error near the Load More area or below the table while keeping the table visible. Do not show the normal empty-state row for initial load failures. Add or update NotificationsView.spec.ts coverage that loads an initial jobs page successfully, makes the next cursor request reject, clicks Load More, then asserts the existing job rows/table remain visible, the pagination error is shown, and the main initial-load jobsError empty-state behavior still hides the table on a fresh load failure.
