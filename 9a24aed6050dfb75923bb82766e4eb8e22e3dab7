lisa-addressed:
---
Comment (src/views/admin/partials/WorkflowImportPanel.vue:76):
`resultSeverity` only goes `error` when `successfulFiles === 0`. A file that parses but whose definitions all fail is `success:true` (file-level), so this yields `warn` while the banner above still says "processed successfully" — mixed signals for a fully-failed import.

Separately, the per-definition failure reason is never surfaced anywhere in the UI: the row just shows `imported N definition(s), M failed` and the only explanation lives in the API logs. Consider rendering `result.message`/failure detail so admins can see *why* a definition failed (e.g. the soft-deleted-row re-import error from the API side).
Replies:
reviewer conflict detected — pausing until resolved.

The request to make a parsed file with definition failures an error-level/fully failed import conflicts with the recorded design decision that a successful file with summary.failed > 0 remains counted as processed and is labeled "Processed with failures" rather than being reclassified as a file failure. It also conflicts with the previously implemented banner feedback in commit 2cb3afb, which deliberately changed the headline to "processed with failures" while preserving warning severity and API success counts. The remaining failure-detail claim appears to be a misunderstanding: src/views/admin/partials/WorkflowImportPanel.vue already renders result.message in the table's Message column.

Please resolve the conflicting feedback and re-request review.

<!-- lisa-conflict-pending -->
This is end-to-end feedback. please address - the behavior is wrong.
Implementation guidance: The human reply explicitly says this end-to-end behavior is wrong, so update src/views/admin/partials/WorkflowImportPanel.vue to distinguish a partial import from an all-failed parsed file. Add a small helper near resultSeverity, for example hasImportedRecords(summary: WorkflowImportSummary), that returns true when any actual imported/updated workflow records are nonzero: definitionsCreated, definitionsUpdated, steps, dependencies, controlRelationships, instances, or roleAssignments. Exclude failed from this helper, and do not use successfulFiles alone as proof of success. Change resultSeverity so it returns 'error' when successfulFiles === 0 OR when results.summary.failed > 0 and hasImportedRecords(results.summary) is false; keep 'warn' for mixed outcomes where failedFiles > 0 or summary.failed > 0 but at least one record was imported/updated. Update resultHeadline to use error-oriented wording for the all-failed parsed case, such as "0 workflow records imported; failures need review" or "Import failed; no workflow records were imported", instead of "processed with failures". Also update fileStatusLabel/fileStatusSeverity so an individual result with success: true, summary.failed > 0, and no imported records is displayed as Failed/danger; keep "Processed with failures"/warn only when that file has both imported records and failures. For failure details, keep rendering result.message but make it explicit and useful for admins: if the API response includes structured failure detail fields, normalize them in src/composables/workflows/useWorkflowImport.ts without breaking camelCase/snake_case handling, render them under the message in the table, and add tests; if the API only provides result.message today, preserve that fallback and ensure the all-failed test uses a realistic message such as the soft-deleted re-import reason and asserts it is visible. Update src/views/admin/partials/__tests__/WorkflowImportPanel.spec.ts to cover the all-failed parsed-file case separately from the existing partial-failure case: all-failed should show an error/Failed outcome and no clean or warning success headline, while mixed imported-plus-failed should remain warning/Processed with failures.

lisa-design:
- A parsed workflow import file with summary.failed > 0 and zero imported or updated workflow records is treated as a failed import in the UI, even if the API reports success: true for the file parse. Files with both imported records and failures remain partial successes and are shown as processed with failures.
