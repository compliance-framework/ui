lisa-addressed:
---
[medium] src/components/catalogs/ControlCreateForm.vue:26 (correctness)
The create form now initializes the optional OSCAL class field to an empty string, so submitting a new control without touching Class sends `class: ""`. The previous `ref({} as Control)` form omitted `class` unless the user entered one, and `ControlEditModal` still treats a blank class as absent. Sending an empty optional OSCAL attribute can cause API validation failures or persist an invalid/meaningless class value for otherwise valid control creation.
Suggested fix: Do not seed optional class with an empty string. Initialize the reactive control without `class`, or build the create payload so `class` is only present when the user entered a value.
Guidance: In src/components/catalogs/ControlCreateForm.vue, update the initial reactive control object so it does not include the optional class key. Keep id and title initialized for the required fields, but remove `class: ''`. Leave the existing v-model on `control.class`; Vue will add the property only after the user enters a value. Do not add trimming, ID/title normalization, endpoint changes, or a generic payload-cleaning helper, because the catalog forms are meant to preserve the existing OSCAL payload shape and only block required-field validation locally. Apply this consistently across all three existing submit paths in createControl by continuing to submit the same `control` object.
---
[medium] src/components/catalogs/GroupCreateForm.vue:45 (correctness)
The create form now initializes the optional group class field to an empty string, so creating a group with Class untouched submits `class: ""`. The prior form omitted the optional property unless the user provided it, and this changes the wire payload for the existing create endpoints.
Suggested fix: Do not seed optional class with an empty string. Initialize the reactive group without `class`, or construct the create payload so `class` is only included when it has a user-entered value.
Guidance: In src/components/catalogs/GroupCreateForm.vue, update the initial reactive group object so it does not include the optional class key. Keep id and title initialized for the required fields, but remove `class: ''`. Leave the existing v-model on `group.class`; Vue will add the property only if the user types into Class. Do not add trimming, class normalization, endpoint changes, or a shared payload sanitizer, because the refactor should preserve existing OSCAL payload objects and endpoint behavior. The fix must apply to both top-level group creation and nested group creation because both submit the same `group` object.
