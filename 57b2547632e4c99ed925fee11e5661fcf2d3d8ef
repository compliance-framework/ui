lisa-addressed:
---
Comment (src/views/control-implementations/__tests__/IndexView.spec.ts:110):
**Low — dead/misleading test scaffolding.** These `callIndex % 3 === 1` / `=== 2` branches (plus the `fetchPendingDashboardSuggestions` and `fetchDashboardSuggestionControlResults` mocks) are unreachable. The component creates exactly one `useDataApi(null)` instance — `sspRisks` — which hits this mock at `callIndex 0` (`0 % 3 === 0`) and falls through to the default `execute: loadRisks` branch. Dashboard suggestions are now fetched via `axios.get` directly (the pass-2 change), so nothing routes through these useDataApi branches.

Beyond being ~40 lines of dead code, it's actively misleading: it implies pending suggestions / control-results flow through `useDataApi`, which they don't. Suggest deleting the `% 3` branches and the two unused mock fns, leaving only the `loadRisks` default for the `url === null` case.
---
Comment (src/views/control-implementations/IndexView.vue:440):
**Low (nit) — unused `force` parameter.** No caller ever passes `force=true` (the four call sites at lines 521, 981, 1034 and the helper all invoke it with no args), so the `!force` bypass branches at lines 453 and 457 can never be skipped. Either wire up a forced-refresh path (e.g. a manual "refresh suggestions" action that should ignore `loadedDashboardSuggestionStateFor`) or drop the parameter to keep the guard logic honest.
Implementation guidance: In src/views/control-implementations/__tests__/IndexView.spec.ts, remove the dead dashboard-suggestion useDataApi mock scaffolding left over from before loadDashboardSuggestionState switched to axios.get. Delete the top-level vi.fn declarations for fetchPendingDashboardSuggestions and fetchDashboardSuggestionControlResults, delete the useDataApiNullCallIndex variable, delete the beforeEach reset of useDataApiNullCallIndex, and delete the beforeEach mockImplementation blocks for those two functions. In the vi.mock('@/composables/axios') useDataApi implementation, simplify the url === null branch so it always returns the sspRisks mock shape: { data: ref([]), isLoading: ref(false), error: ref(null), execute: loadRisks }. Do not touch axiosGet, pendingDashboardSuggestionsFixture, controlResultsFixture, or the reject flags; current dashboard-suggestion tests correctly drive the component through useAuthenticatedInstance().get. After cleanup, run the IndexView spec and fix any remaining references to the deleted mocks rather than reintroducing the old useDataApi branches.
Implementation guidance: In src/views/control-implementations/IndexView.vue, remove the unused force parameter from loadDashboardSuggestionState. Change the signature from async function loadDashboardSuggestionState(force = false) to async function loadDashboardSuggestionState(). In that function, make the existing cache and in-flight guards unconditional: if (loadedDashboardSuggestionStateFor.value === sspId) return; and if (loadingDashboardSuggestionStateFor.value === sspId && dashboardSuggestionStateLoadPromise) { await dashboardSuggestionStateLoadPromise; return; }. Do not remove the loadedDashboardSuggestionStateFor guard, loadingDashboardSuggestionStateFor guard, request-id freshness check, Promise.allSettled partial-failure handling, or the non-blocking initializeDashboardSuggestionState behavior; those were added to address prior concurrency findings. No call sites need replacement because all existing calls already pass no argument. Run the IndexView tests after the change.
