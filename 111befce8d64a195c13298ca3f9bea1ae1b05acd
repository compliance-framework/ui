lisa-addressed:
---
Comment (src/router/index.ts:427):
**[Medium] `requiresAdmin` is dead metadata — the admin gate is not actually enforced.** The new `meta.requiresAdmin: true` here has no effect: `router.beforeEach` (src/router/index.ts:1031) only evaluates `requiresAuth` — there is no admin/role predicate anywhere in this client. So `/admin/import` (including this new workflow importer) remains reachable by *any* authenticated user, exactly as before. This isn't a regression versus the pre-existing OSCAL importer on the same surface, but the flag reads as if access is restricted when it isn't. Either (a) add an admin check to `beforeEach` backed by a real user/role model, or (b) drop the `requiresAdmin` flag and the assertion that tests it until such a model exists, so the code doesn't imply a guarantee it can't keep.
---
Comment (src/router/__tests__/index.spec.ts:22):
**[Low] This assertion gives false confidence.** It asserts `meta.requiresAdmin === true`, but nothing in the router enforces that flag (`beforeEach` only checks `requiresAuth`). A green test here reads as 'admin access is restricted' when it only confirms a string of metadata exists. Tie this to behavior instead — e.g. assert that navigating to `/admin/import` as a non-admin is redirected — or remove it along with the flag (see the thread on router/index.ts:427).
---
Comment (src/views/admin/partials/WorkflowImportPanel.vue:316):
**[Low] Duplicate-filename key collision in the results table.** `:key="result.filename"` assumes filenames are unique, but the backend returns one row per uploaded file and a user can legitimately select two files with the same name (from different folders). Colliding `:key`s cause Vue to reuse/mis-render rows. The selected-files list above already avoids this with a composite key (`\`${file.name}-${file.size}-${index}\``); do the same here, e.g. `:key="\`${result.filename}-${index}\`"` using the v-for index.
---
Comment (src/composables/workflows/useWorkflowImport.ts:134):
**[Low] 413 message hardcodes the limits, decoupled from the source of truth.** This string bakes in 'at most 10 files, 5 MB each', while the actual guards live as `MAX_FILES = 10` and `MAX_FILE_SIZE_BYTES = 5 * 1024 * 1024` in WorkflowImportPanel.vue:16-17. If the limits ever change in one place, this message silently goes stale. Consider exporting the limits as shared constants and interpolating them here (and in the panel's validation messages) so the numbers can't drift.
---
Comment (src/utils/httpErrors.ts:53):
**[Low] `!== undefined` lets a `null` body through as the error detail.** The guard `responseData.errors?.body !== undefined` returns `responseData.errors.body` even when it's `null`, so the function (typed `Promise<string>`) can resolve to `null`; callers like `new Error(detail)` then render the literal string 'null'. The previous code coalesced this via `?? (message || fallback)`, so a null body fell through to the message. Use a truthiness check (`if (responseData?.errors?.body) return responseData.errors.body;`) to preserve the old fallback behavior.
Implementation guidance: In src/router/index.ts, update the authenticatedRoutes entry for name 'admin-import' at path '/admin/import': remove meta.requiresAdmin and keep meta.requiresAuth: true. Do not add an admin check to router.beforeEach unless there is already a real admin/role capability in the Pinia auth store/session model; there is not one in this client today. Do not move the route or create a new workflow import route, because the importer is deliberately mounted on the existing '/admin/import' surface. Authorization beyond authentication should remain enforced by the backend until the client has a real admin group model.
Implementation guidance: In src/router/__tests__/index.spec.ts, update the 'keeps the import surface under the admin authenticated route pattern' test so it no longer asserts route.meta.requiresAdmin === true. Keep assertions that the route named 'admin-import' has path '/admin/import' and meta.requiresAuth === true. If TypeScript allows it cleanly, add expect(route?.meta.requiresAdmin).toBeUndefined() to document that the client does not currently enforce an admin route flag; otherwise just delete the requiresAdmin assertion. Do not add a navigation redirect test for non-admin users unless a real admin predicate is implemented first.
Implementation guidance: In src/views/admin/partials/WorkflowImportPanel.vue, change the results table row v-for from 'v-for="result in results.results"' to include the index, e.g. 'v-for="(result, index) in results.results"', and change the row key from ':key="result.filename"' to a composite key such as ':key="`${result.filename}-${index}`"'. Only change the outer result row key; leave the inner summaryFields v-for key as field.key. Do not assume filenames are unique, because users can upload same-named files from different folders.
Implementation guidance: Create a small shared constants module for workflow import limits, for example src/composables/workflows/workflowImportLimits.ts, exporting WORKFLOW_IMPORT_MAX_FILES = 10, WORKFLOW_IMPORT_MAX_FILE_SIZE_BYTES = 5 * 1024 * 1024, and WORKFLOW_IMPORT_MAX_FILE_SIZE_MB = WORKFLOW_IMPORT_MAX_FILE_SIZE_BYTES / 1024 / 1024. In src/views/admin/partials/WorkflowImportPanel.vue, remove the local MAX_FILES and MAX_FILE_SIZE_BYTES constants and import the shared constants. Use WORKFLOW_IMPORT_MAX_FILES in the selected-file count guard and helper text, use WORKFLOW_IMPORT_MAX_FILE_SIZE_BYTES in the oversized-file filter, and interpolate WORKFLOW_IMPORT_MAX_FILE_SIZE_MB in messages currently hardcoding '5 MB'. In src/composables/workflows/useWorkflowImport.ts, import the same constants and build the 413 Error message with WORKFLOW_IMPORT_MAX_FILES and WORKFLOW_IMPORT_MAX_FILE_SIZE_MB instead of hardcoded '10' and '5 MB'. Keep the actual limits unchanged and do not add client-side JSON parsing or content validation.
Implementation guidance: In src/utils/httpErrors.ts, inside getErrorDetail's Axios/non-Response branch, change the errors.body guard so null does not get returned as the detail. Replace the current condition checking responseData.errors?.body !== undefined with a truthy string-style guard, e.g. if (responseData && 'errors' in responseData && responseData.errors?.body) { return responseData.errors.body; }. This preserves fallback behavior for null, undefined, and empty string, allowing message/error/detail/errorResponse.message/fallbackMessage to be used. Do not change getErrorStatus or the Response branch unless tests reveal the same bug there.
