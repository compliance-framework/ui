lisa-addressed:
---
Comment (src/views/dashboard/IndexView.vue:13):
**[High] Dashboard-suggestions entry point is in the wrong place.** This puts the 'AI suggestions' affordance on the global `/dashboards` index behind an SSP-picker dialog (`showSspPicker`). The expected home is the per-control **Control Implementation** editor, right beside the existing component-suggestions feature and the 'Evidence Linking' section — see `src/views/control-implementations/partials/ControlStatementImplementation.vue`: the `ControlStatementSuggestions` block (~line 1407) and the Evidence Linking section (~line 1451). That surface already has the SSP in scope (`resolvedSspId` / `system.securityPlan?.uuid`), so the separate SSP picker is unnecessary — suggestions would scope to the SSP the user is already editing, exactly like component suggestions do.

Suggestion: surface the dashboard-suggestions entry point inside `ControlStatementImplementation.vue` next to `ControlStatementSuggestions` (gated on `aiConfig.dashboardSuggestionsEnabled`), passing the in-context SSP id, and reconsider whether the global `/dashboards` button + SSP picker should remain at all.
---
Comment (src/views/dashboard/IndexView.vue:14):
**[Medium] When AI isn't configured the feature vanishes silently — no message.** `v-if="aiConfig.dashboardSuggestionsEnabled"` hides the entry point entirely when the config probe returns disabled — which is also the fail-closed state when no AI provider is configured. The user is given no indication that AI suggestions exist but require configuration. The only 'disabled' copy lives in `SuggestionsView.vue:27` ('Dashboard suggestions are disabled.'), but that page is reachable *only via this button*, so when disabled it's unreachable and the message never shows.

Suggestion: when `dashboardSuggestionsConfigFetched && !dashboardSuggestionsEnabled`, render a short inline note at the entry point (a `Message` or a disabled button + tooltip) telling the user AI isn't configured so suggestions can't be generated — rather than removing the control. Apply the same treatment wherever the entry point ultimately lives per the placement comment above.
---
Comment (src/views/dashboard/SuggestionsView.vue:27):
**[Low] This disabled-state copy is effectively unreachable, and conflates two distinct states.** This 'Dashboard suggestions are disabled.' branch only renders on `/dashboards/suggestions/:sspId`, which today is reachable only through the entry-point button that is hidden when disabled (IndexView.vue:14) — so users never see it. It also can't distinguish 'admin turned the feature off' from 'no AI provider configured', because `/api/dashboard-suggestions/config` returns only `{ enabled }`.

Suggestion: make the copy explicit ('AI is not configured, so suggestions can't be generated') and ensure it renders wherever the entry point lives. Consider extending the config endpoint with a reason/configured flag so the UI can word this precisely instead of guessing.
Implementation guidance: Move or add the dashboard-suggestions entry point to src/views/control-implementations/partials/ControlStatementImplementation.vue near the existing ControlStatementSuggestions block and Evidence Linking section. Use the SSP already in scope there, preferring resolvedSspId and falling back to system.securityPlan?.uuid if that matches local patterns, and route to the existing suggestions page with that SSP id. Gate the enabled action with the cached aiConfig.dashboardSuggestionsEnabled value; do not add a new per-component config probe. Remove the global /dashboards SSP-picker entry point in src/views/dashboard/IndexView.vue if it becomes redundant, or keep only if product wants a secondary global launcher, but do not require users in the control implementation editor to pick an SSP again.
Implementation guidance: Wherever the dashboard-suggestions entry point is rendered after addressing PRRT_kwDONe0Wms6J1ICj, show an explicit disabled state once aiConfig.dashboardSuggestionsConfigFetched is true and aiConfig.dashboardSuggestionsEnabled is false. Use a short Message or disabled affordance with copy such as "AI is not configured, so dashboard suggestions cannot be generated." Keep the feature fail-closed, but avoid hiding the entry point silently. If the global /dashboards entry point remains in src/views/dashboard/IndexView.vue, apply the same disabled-state treatment there too.
Implementation guidance: Update the disabled-state copy in src/views/dashboard/SuggestionsView.vue from the generic "Dashboard suggestions are disabled." wording to explicit AI-configuration wording consistent with the entry point, for example "AI is not configured, so dashboard suggestions cannot be generated." Also make sure the same disabled message is reachable at the actual entry point implemented for PRRT_kwDONe0Wms6J1ICj. Do not block this UI fix on extending /api/dashboard-suggestions/config with a reason/configured field; the current frontend contract only exposes enabled, so phrase the message around unavailable AI configuration unless a backend contract already exists.
