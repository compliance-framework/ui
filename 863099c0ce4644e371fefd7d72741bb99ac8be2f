lisa-addressed:
---
Comment (src/views/system-security-plans/partials/StatementByComponent.vue:45):
**[Medium] 340-line fork of `StatementByComponent` leaves the buggy original alive.**

This file is a near-complete copy of `src/views/control-implementations/partials/StatementByComponent.vue`, and the correctness fixes from the self-review passes — draft cloning here instead of prop mutation, and the guarded `componentUrl` fetch — were applied **only to this copy**. The original is still live via `ControlStatementImplementation.vue`: it still mutates `props.byComponent` by reference while editing (unsaved edits leak into the parent tree on Cancel) and still builds its fetch URL from `useSystemStore().system.securityPlan?.uuid as string`, so it can request `/system-security-plans/undefined/...`.

I see from the git notes the fork was deliberate (control-implementations is a separately owned surface), so narrowing the ask: extract the shared logic — `relatedRisks` matching, `highestSeverity`, the status editor computeds, and the clone-draft pattern — into a shared module both partials consume (the URL can be a prop), **or** port the two fixes back to the original in a follow-up. Two diverging copies of ~340 identical lines, where one carries known bug fixes the other lacks, is where the next regression hides.
---
Comment (src/components/system-security-plans/SystemImplementationUserCreateForm.vue:58):
**[Medium] This restyles a pre-existing broken/duplicated form block instead of removing it.**

This `v-for` over `userData.roleIds` renders a **readonly User UUID field + Generate button once per role ID** — clearly a copy-paste error, since the real Role IDs editor (bound to `userData.roleIds![index]`) appears further down at line ~110. On top of that, the entire Title / Short Name / Description block is rendered **twice** (lines 23–47 and again at 73–99, nested inside this Role IDs `<div>`), both copies bound to the same `userData` fields, so the Create User modal visibly shows every field duplicated.

The bug predates this PR, but the diff touches these exact lines (the `FormInput → InputText` swap), and a refactor whose purpose is cleaning up these forms is the right moment to delete the duplicate block: drop this bogus loop (lines ~52–70) and un-nest/de-duplicate the second Title/Short Name/Description/Role IDs section so each field renders once.
---
Comment (src/views/system-security-plans/__tests__/sspViews.spec.ts:155):
**[Low] Two assertions here can't fail the way they intend to.**

1. `expect(wrapper.text()).toContain('Active')` is already satisfied by the **"Set Active"** button text, so it would pass even if the Active badge were removed entirely. Assert on the badge specifically, e.g. stub `Badge` with a distinct marker (`data-testid="active-badge"`) and assert it renders exactly once, on the active row.

2. The blocked-submit test's `apiCalls.every((c) => c.execute.mock.calls.length === 0)` (line 122) is vacuously true if `apiCalls` is empty — if the `useDataApi` mock wiring ever breaks, the test keeps passing while asserting nothing. Precede it with `expect(apiCalls.length).toBeGreaterThan(0)` so the existence precondition is pinned.
Implementation guidance: Valid feedback. Use the minimal safe path: port the two correctness fixes from src/views/system-security-plans/partials/StatementByComponent.vue back into src/views/control-implementations/partials/StatementByComponent.vue, but do not refactor risk widgets or routes. In the control-implementations partial, replace the prop-reference draft with a cloned draft: add a cloneByComponent(value: ByComponent): ByComponent helper using JSON.parse(JSON.stringify(value)) or structuredClone if supported; initialize localComponent from a clone; reset it from a fresh clone when entering edit and on cancel; emit normalizeByComponentImplementationStatus(cloneByComponent(localComponent.value)) on save; emit props/byComponent, not a dirty draft, on delete. Also change read mode to render the persisted byComponent description/status instead of the draft so failed saves cannot display unsaved data. For the component fetch, replace the eager useDataApi URL string with a computed componentUrl that returns null unless system.securityPlan?.uuid and byComponent.componentUuid are both present, call useDataApi<SystemComponent>(null, { method: 'GET' }, { immediate: false }), and watch componentUrl with { immediate: true } to call execute/fetchComponent only for a non-null URL. Keep the existing @save/@delete event names and payload shape, keep route names and store semantics, and do not alter RiskIndicatorBadge behavior beyond continuing to guard it with an available SSP uuid. Add tests for the original control-implementations partial proving Cancel does not mutate the parent byComponent object and no /system-security-plans/undefined/... fetch is issued when there is no active securityPlan.
Implementation guidance: Valid feedback. In src/components/system-security-plans/SystemImplementationUserCreateForm.vue, clean up the template so each create-user field appears exactly once. Keep the top UUID field with Generate, then one Title field, one Short Name field, one Description textarea, one Role IDs editor, and the Authorized Privileges section. Delete the bogus first Role IDs block that loops over userData.roleIds but renders a readonly InputText bound to userData.uuid plus a Generate button. Also delete the duplicated nested Title/Short Name/Description block. The only Role IDs v-for should bind each input to userData.roleIds![index], include the Remove button, and keep the Add Role ID button. Be careful with the surrounding <div class='mb-4'> nesting so Authorized Privileges and the submit/cancel buttons remain inside the form but not inside a duplicated Role IDs container. Do not change the POST endpoint, createUser(), emitted events, or the userData payload fields.
Implementation guidance: Valid test feedback. In src/views/system-security-plans/__tests__/sspViews.spec.ts, make the assertions non-vacuous. First, update the Badge stub so the active badge has a distinct marker, for example props ['value'] and template '<span :data-testid="value === \'Active\' ? \'active-badge\' : \'badge\'">{{ value }}<slot /></span>'. Then in the 'renders the list create button, active badge...' test, replace expect(wrapper.text()).toContain('Active') with an assertion on wrapper.findAll('[data-testid="active-badge"]') having length 1. If practical, also assert the active badge is in the row/card containing 'Active SSP' and not in the row/card containing 'Inactive SSP'; do not rely on the Set Active button text. Second, in the blocked-submit test, before apiCalls.every(...), add expect(apiCalls.length).toBeGreaterThan(0), or better find the create endpoint call with apiCalls.find((apiCall) => apiCall.url === '/api/oscal/system-security-plans'), assert it exists, and assert createCall.execute was not called. Keep the existing mock endpoint and route assertions unchanged.
